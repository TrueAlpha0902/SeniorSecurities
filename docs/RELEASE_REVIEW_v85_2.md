# SeniorSecurities v85.2 發布前審查

## 變更範圍

- 選項編號改為純文字，取消圓形或膠囊背景。
- 正確／錯誤狀態改以文字標記，移除勾號與叉號圖示。
- 保留整張深綠／深紅的答案回饋。
- 證券高業、初階外匯及相似題共用一致顯示。
- 證券練習計時器改為融入題號列；暫停按鈕改為透明控制。
- 初階外匯模擬考計時器改為透明區塊；急迫狀態仍保留警示。

## 資料影響

未修改任何題幹、選項、答案、解析、題目 ID、會員權限或 Supabase schema。

## 驗證

已完成：

- 三個 TSX 檔案 TypeScript 語法轉譯。
- 靜態契約：無 `<Check>`／`<X>` 答案圖示，均使用 `answer-result-label`。
- CSS 維護預算：10 個檔案、9,737 行、212 個 `!important`、`glass.css` 4,088 行。
- 手機版 412px 靜態視覺預覽。

受限項目：

- 此容器的內部 npm registry 在驗證期間回傳 HTTP 503，因此未重新安裝依賴或執行完整 `npm run verify`。
- 更新器會在使用者 Windows 專案中執行完整型別、Lint、契約、Production build 與 Bundle 檢查；任何失敗都會停止。

## 發布狀態

v85.2 尚未部署。
